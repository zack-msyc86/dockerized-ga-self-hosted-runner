version: '3'
services:
  runner:
    build: .
    restart: unless-stopped
    environment:
      ACCESS_TOKEN: ${ACCESS_TOKEN}
      OWNER: ${OWNER}
      REPO: ${REPO}
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    deploy:
      replicas: 4
    privileged: true